{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:fixtures": "node ./src/test-fixtures.mjs",
    "test:repos": "node ./src/test-migrator.mjs",
    "migrate": "node ./src/cli.mjs"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
