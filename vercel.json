{
  "framework": "vite",
  "buildCommand": "pnpm --filter @workspace/solana-migrator build",
  "outputDirectory": "artifacts/solana-migrator/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
